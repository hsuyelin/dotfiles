# ============================================================
# Claude (XDG-ish)
# ============================================================

# Claude uses this directory for its configuration and runtime files. Keep it
# out of $HOME to avoid scattering application state across the home directory.
export CLAUDE_CONFIG_DIR="$HOME/.local/share/claude"

# ============================================================
# zoxide — XDG data dir only
# (init is done in .zshrc after zi and fzf are ready)
# ============================================================
export _ZO_DATA_DIR="$XDG_DATA_HOME/zoxide"

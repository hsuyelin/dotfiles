# ============================================================
# OMP
# ============================================================

# Keep OMP configuration and agent state under the XDG data directory.
export PI_CONFIG_DIR=".local/share/omp"

export PI_STREAM_FIRST_EVENT_TIMEOUT_MS=600000
export PI_STREAM_IDLE_TIMEOUT_MS=600000
export PI_OPENAI_STREAM_FIRST_EVENT_TIMEOUT_MS=600000
export PI_OPENAI_STREAM_IDLE_TIMEOUT_MS=600000

# ============================================================
# CodeGraph
# ============================================================
# Keep the npm launcher cache and bundled runtime under the XDG data root.
export CODEGRAPH_INSTALL_DIR="$XDG_DATA_HOME/codegraph"
